<ul>
  <% if Admin::SchoolsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_schools_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_school_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::SchoolsController.sortable? and School.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Schools"),
                 admin_schools_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Schools"),
                 admin_schools_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>