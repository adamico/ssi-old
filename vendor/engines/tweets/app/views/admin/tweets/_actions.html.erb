<ul>
  <% if Admin::TweetsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_tweets_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_tweet_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::TweetsController.sortable? and Tweet.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Tweets"),
                 admin_tweets_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Tweets"),
                 admin_tweets_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>