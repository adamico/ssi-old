<ul>
  <% if Admin::LinksController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_links_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_link_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::LinksController.sortable? and Link.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Links"),
                 admin_links_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Links"),
                 admin_links_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>