<ul>
  <% if Admin::LinkCategoriesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_link_categories_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_link_category_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::LinkCategoriesController.sortable? and LinkCategory.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Link Categories"),
                 admin_link_categories_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Link Categories"),
                 admin_link_categories_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>