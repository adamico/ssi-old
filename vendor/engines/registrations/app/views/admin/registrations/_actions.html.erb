<ul>
  <% if Admin::RegistrationsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_registrations_path
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_registration_path,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::RegistrationsController.sortable? and Registration.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Registrations"),
                 admin_registrations_path,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Registrations"),
                 admin_registrations_path,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>